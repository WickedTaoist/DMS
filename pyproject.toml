[project]
name = "driver-phone-usage"
version = "0.1.0"
description = "Research prototype for driver phone usage detection."
requires-python = ">=3.10"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
target-version = "py310"
