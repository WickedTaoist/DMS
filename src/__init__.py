"""
项目源码包标记 / Package marker for the project source tree.

该文件本身不承载业务逻辑，主要作用是：
1. 告诉 Python 解释器 `src/` 是一个可导入包。
2. 允许使用 `python -m src.main` 的模块运行方式。

This file does not contain business logic.
Its main purposes are:
1. Mark `src/` as an importable Python package.
2. Enable module-style execution such as `python -m src.main`.
"""
